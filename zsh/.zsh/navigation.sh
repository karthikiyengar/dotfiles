# I use Ctrl + D in Vim to scroll. Don't want to kill the terminal accidentally.
set -o ignoreeof

# Enable autocd - just type directory name to cd into it
setopt autocd

# Note: Keybinds have been consolidated into keybinds.sh
